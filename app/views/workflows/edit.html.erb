<!-- 플러그인 테스트: 이 파일이 로드되면 뷰 오버라이드가 작동하는 것입니다 -->
<div style="background: #ff6b6b; color: white; padding: 10px; margin: 10px 0; border-radius: 5px;">
  <h2>🚀 WORKFLOW VIZ PLUGIN VIEW OVERRIDE WORKING!</h2>
  <p>This view is overridden by the plugin.</p>
  <p>Time: <%= Time.current %></p>
</div>

<!-- 원본 뷰 내용을 렌더링 -->
<%= render :file => File.join(Rails.root, 'app', 'views', 'workflows', 'edit.html.erb') rescue "Original view not found" %>

<!-- 플러그인 시각화 추가 -->
<%= render 'workflow_visualization' %>

<script>
console.log('Workflow Viz: View override loaded');
</script>
